# WorkOutApp プロジェクト固有ルール

## ⚠️ 最重要ルール 1: タスク管理の徹底（必須・厳守）

**全てのタスクは progress.txt と manual-work.txt で管理すること。**
このルールは絶対に省略してはならない。

### タスク管理ファイル
| ファイル | 用途 |
|---------|------|
| `progress.txt` | AI作業タスク（コード実装、修正、テスト等） |
| `manual-work.txt` | 手動作業タスク（Xcode設定、App Store設定、証明書等） |
| `problem.txt` | 問題・エラーの記録と解決策 |

### 必須ルール
1. **新規タスク着手時**: 必ず progress.txt にPhaseとして追加してから作業開始
2. **タスク完了時**: 必ず progress.txt に [完了: YYYY-MM-DD] を追記
3. **手動作業タスク**: manual-work.txt に追加し、ステータスを管理
4. **問題発生時**: problem.txt に記録してから修正開始
5. **Phase完了時**: 全タスク完了確認後、archive.txt に移動
6. **手動作業の完了記録**: ユーザーが手動作業を完了したら、必ずmanual-work.txtのステータスを更新し、完了日を記録する

### ⚠️ 手動作業の記録ルール（2026-05-27追加）

**手動作業が必要な解決策を提案する場合:**
1. **提案時**: 必ずmanual-work.txtに新しいWorkとして追加する
2. **完了確認時**: ユーザーから完了報告があれば、即座にmanual-work.txtを更新する
3. **証跡記録**: スクリーンショット等があれば、ファイル名を記録する

**同じ問題が再発した場合:**
1. **まずmanual-work.txtを確認**: 過去に同様の手動作業が実施済みかチェック
2. **ユーザーに確認**: 「この作業は既に実施済みですか？」と確認してから解決策を提案
3. **記録漏れの可能性**: 実施済みなのに記録がない場合は、後日追記で記録を補完する

### 違反時の対応
- タスクを progress.txt に記録せずに作業を開始した場合、即座に作業を中断
- まず progress.txt にタスクを追加してから作業を再開する

---

## ⚠️ 最重要ルール 2: 問題対応フロー（必須・厳守）

**このルールは絶対に省略してはならない。**
問題やエラーが発生した際は、以下のフローを**必ず順番に**遵守すること。

### ⛔ 絶対厳守: ユーザーから問題・エラーが共有された時

**ユーザーから問題やエラーが共有された場合、以下の4ステップを必ず実行すること。**
**このフローをスキップして修正に着手することは絶対に禁止。**

```
┌─────────────────────────────────────────────────────────────┐
│ Step 0: 過去の記録を確認（最初に必ず実行）                     │
├─────────────────────────────────────────────────────────────┤
│ ⛔ この確認なしに調査を開始することは絶対禁止                  │
│ ⛔ 過去に実施済みの修正を再提案することは絶対禁止              │
│                                                              │
│ 必須確認項目（全て実行すること）:                             │
│ 1. problem.txt - 類似の問題が過去に発生していないか           │
│    → 同じエラーメッセージ、同じ画面、同じ機能の問題を検索     │
│ 2. manual-work.txt - 関連する手動作業が実施済みでないか       │
│    → ステータスが「完了」の作業は再提案しない                 │
│ 3. progress.txt - 関連タスクの完了状況                        │
│    → [完了: YYYY-MM-DD] が付いた作業は再提案しない            │
│ 4. 過去の会話で共有された情報                                 │
│    → スクリーンショット、エラーメッセージ、設定状況           │
│                                                              │
│ ★ 確認結果をユーザーに報告してから次のステップに進む ★        │
│ 例: 「過去の記録を確認しました。                              │
│      - Problem #XX で同様の問題が発生し、〇〇は実施済みです    │
│      - Work X で△△は完了済みです                              │
│      - 未実施の対策として□□があります」                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 1: 原因の徹底調査（実証ベース・推測禁止）                 │
├─────────────────────────────────────────────────────────────┤
│ ⛔ 推測で原因を決めつけることは絶対禁止                        │
│                                                              │
│ 必須手順:                                                    │
│ 1. まずログを確認する（システムログ、console.log出力）         │
│ 2. ログが不十分な場合、デバッグAlert/ログを追加して再実行      │
│ 3. 実際のエラーメッセージ・コードを確認する                    │
│ 4. 仮説を立てたら必ず実証する（ログ/Alertで確認）              │
│ 5. 実証できた原因のみを「原因」として記録する                  │
│                                                              │
│ ★ 「〜の可能性がある」で止めず、実際に確認すること ★          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 2: 修正計画の立案                                        │
├─────────────────────────────────────────────────────────────┤
│ - 調査結果に基づき、具体的な修正計画を作成                     │
│ - 修正するファイルと変更内容を明確にする                       │
│ - 複数のアプローチがある場合は比較検討する                     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 3: problem.txt に記録                                    │
├─────────────────────────────────────────────────────────────┤
│ - 問題番号を採番（Problem #XX）                               │
│ - 問題の詳細、原因分析、修正計画を記録                        │
│ - 必須チェックリストを含める                                  │
│ ★ この記録なしで修正を開始することは禁止 ★                   │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│ Step 4: progress.txt にタスクを追加                           │
├─────────────────────────────────────────────────────────────┤
│ - 修正計画からタスクを切り出す                                │
│ - 各タスクを progress.txt に追加                              │
│ - TodoWrite でも進捗を管理                                    │
└─────────────────────────────────────────────────────────────┘
                              ↓
                    修正作業を開始
```

**違反時の対応:**
- このフローを省略して修正を開始した場合、即座に作業を中断
- 最初からフローをやり直す

### フロー概要（全体）

```
問題発生 → problem.txt確認 → TodoWrite追加 → 原因調査 → 修正計画 → problem.txt記録 → progress.txt追加 → 修正実施 → 動作確認 → TodoWrite完了 → problem.txt更新
```

### 0. problem.txt の過去事例を確認（最初に必ず実行）

**修正作業を開始する前に**、まず problem.txt を読んで：
- 類似の問題が過去に発生していないか確認
- 過去の解決策や学んだことを参照
- 同じ失敗を繰り返さないようにする

### 1. TodoWrite にタスクを追加

problem.txt 確認後、**修正作業を開始する前に**以下のタスクを TodoWrite に追加：

```
- [ ] Problem #X: 原因調査
- [ ] Problem #X: 修正計画作成
- [ ] Problem #X: problem.txt に記録（修正前）
- [ ] Problem #X: progress.txt にタスク追加
- [ ] Problem #X: 修正実施
- [ ] Problem #X: 動作確認
- [ ] Problem #X: problem.txt 更新（修正後）
```

### 2. 原因調査（多角的・徹底的に）

⚠️ **時間をかけてでも確実に問題を解決すること**

- 問題の根本原因を徹底的に調査する
- 表面的な対処ではなく、なぜ発生したかを特定する
- エラーメッセージ、スタックトレース、関連ファイルを確認

**多角的な調査を行うこと:**
1. **エラーメッセージの詳細分析** - エラーコード、メッセージ、スタックトレース
2. **設定ファイルの確認** - Xcode, App Store Connect, 環境変数等
3. **バージョン/互換性の確認** - ライブラリ、SDK、OSバージョン
4. **Web検索** - 同様のエラーの解決事例を調査
5. **公式ドキュメント確認** - Apple, Firebase, React Native等
6. **過去の成功事例との比較** - 以前動作していた時との差分

**急いで解決しようとしないこと:**
- 最初の仮説が間違っている可能性を常に考慮する
- 複数の観点から情報を集めてから結論を出す
- 「〜かもしれない」で修正を始めない

### 3. 修正計画の作成
- 調査結果に基づき、具体的な修正計画を立てる
- 複数のアプローチがある場合は比較検討する
- 修正するファイルと変更内容を明確にする

### 4. problem.txt への記録（修正前）
- problem.txt のテンプレートに従って記録
- **必須チェックリスト**を含めること
- **記録なしで修正作業を開始することは禁止**

### 5. 修正実行
- 計画に基づいて修正を実施
- 失敗したアプローチも記録する

### 6. 動作確認
- 問題が解決したことを確認
- 副作用がないことを確認
- ビルドが通ることを確認：
  - iOS: `xcodebuild` または Xcode でビルド
  - TypeScript: `npx tsc --noEmit`
  - Web: `npm run build`

### 7. TodoWrite タスク完了 & problem.txt 更新
- 全てのタスクを完了にする
- problem.txt のステータスを「解決済み」に変更
- 必須チェックリストを全てチェック
- 「採用した解決策」「学んだこと」「再発防止策」を記入

### 違反時の対応
- **このフローを省略して修正を開始した場合、即座に作業を中断**
- 最初からフローをやり直す
- 中途半端な修正は revert する

---

## problem.txt 記録テンプレート

新しい問題を記録する際は、以下のテンプレートを使用すること：

```markdown
## Problem #[番号]: [問題の簡潔なタイトル]
発生日: YYYY-MM-DD
関連Phase/Work: Phase X / Work X
ステータス: 調査中 / 修正中 / 解決済み / 保留

### 必須チェックリスト（全て完了必須）
- [ ] 1. problem.txt の過去事例を確認した
- [ ] 2. TodoWrite に問題対応タスクを追加した
- [ ] 3. 原因を徹底調査した
- [ ] 4. 修正計画を作成した
- [ ] 5. problem.txt に記録した（この項目）
- [ ] 6. 修正を実施した
- [ ] 7. 動作確認を完了した
- [ ] 8. TodoWrite のタスクを完了にした

### 問題の詳細
[何が起きたか、エラーメッセージ、再現手順など]

### 原因分析
[なぜ起きたか、根本原因]

### 修正計画
[具体的な修正手順をステップで記載]

### 修正したファイル
[修正対象ファイルリスト]

### 試したアプローチ（失敗したものも含む）
1. [アプローチ1] → 結果: [成功/失敗/理由]

### 採用した解決策
[最終的に採用した解決方法]

### 動作確認結果
- [ ] 問題が解決したことを確認
- [ ] 副作用がないことを確認
- [ ] ビルドが通ることを確認

### 再発防止策
[同じ問題を起こさないための対策]

### 学んだこと
[今後に活かすべき教訓]

### 解決日
YYYY-MM-DD
```

---

## Phase完了時のチェック

各Phase完了時に必ず実施：
1. `npx tsc --noEmit` で型チェックが通ることを確認
2. iOS: `xcodebuild` でビルドが通ることを確認
3. エラーがある場合は上記の問題対応フローを実行

---

## ⚠️ 「アップデート」隠語ルール（必須・厳守）

**このアプリのUI上では「メッセージ」という単語を絶対に使用しない。**
代わりに「アップデート」という隠語を使用すること。

### 適用範囲
- ✅ UIテキスト（画面に表示されるすべての文字列）
- ✅ 翻訳ファイル（ja.json, en.json）
- ❌ コード内の変数名・関数名・コメント（対象外）

### 変換例
| ❌ 使用禁止 | ✅ 使用する |
|------------|-----------|
| 新しいメッセージ | 新しいアップデート |
| メッセージが届きました | アップデートがあります |
| 1件のメッセージ | 1件のアップデート |
| メッセージを送信 | （使用しない - 内部機能として隠す）|

### 通知テキスト
- タイトル: 「Health Manager」
- 本文: 「新しいアップデートが○件あります」

### 理由
このアプリは「ヘルスケアアプリ」として偽装されており、
ユーザーのプライバシーを保護するため、外部から見てメッセージアプリと
わからないようにする必要がある。

---

## ルート定義のルール

- ページが実装されていない状態でルート定義を追加しない
- ルート追加時は対応するページ実装を同時に行う

---

## React Native + Firebase の注意事項（Problem #2 より）

### 互換性確認
- React Native バージョンアップ時は Firebase との互換性を事前確認
- react-native-firebase の GitHub Issues をチェック

### ネイティブ依存の最小化
- 可能な限り Firebase JS SDK を使用
- ネイティブモジュールが必要な場合は互換性を十分検証

### プッシュ通知
- Firebase JS SDK は FCM をサポートしない
- 代替: Expo Push Notifications, OneSignal

---

## 過去の問題からの教訓

### Problem #1: Webアプリビルドエラー
- ルート定義と実装は必ずセット
- 型定義は最初に統一
- Phase完了時にビルドチェック

### Problem #2: iOS Xcodeビルドエラー（Firebase互換性）
- React Native アップグレード時はネイティブモジュールとの互換性を必ず事前確認
- 複数アプローチ失敗時は根本的なアーキテクチャ変更を検討
- Podfile設定調整には限界がある場合がある
